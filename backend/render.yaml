services:
  - type: web
    name: ai-app-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python smart_main.py"
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: HOST
        value: 0.0.0.0
